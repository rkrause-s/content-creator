---
interface Props {
  status: string;
}

const { status } = Astro.props;
const normalized = (status ?? "draft").toLowerCase();
---

<span class={`badge badge-${normalized}`}>{status ?? "draft"}</span>
